 <!-- Other Trainings -->
            <h4 class="mt-4">Other Trainings</h4>
            <table class="table table-bordered">
                <tr>
                    <th>Training</th>
                    <th>Institution</th>
                    <th>Year of Completion</th>
                </tr>
                @foreach($application->applicant->trainings as $training)
                <tr>
                    <td>{{ $training->course_name }}</td>
                    <td>{{ $training->institution }}</td>
                    <td>{{ $training->year_of_completion }}</td>
                </tr>
                @endforeach
            </table>

            <!-- Work Experience -->
            <h4 class="mt-4">Work Experience</h4>
            <table class="table table-bordered">
                <tr>
                    <th>Company</th>
                    <th>Position</th>
                    <th>Duration</th>
                </tr>
                @foreach($application->applicant->work_experiences as $work)
                <tr>
                    <td>{{ $work->company }}</td>
                    <td>{{ $work->position }}</td>
                    <td>{{ $work->start_date }} - {{ $work->end_date ?? 'Present' }}</td>
                </tr>
                @endforeach
            </table>

            <!-- Referees -->
            <h4 class="mt-4">Referees</h4>
            <table class="table table-bordered">
                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Company</th>
                    <th>Contact</th>
                </tr>
                @foreach($referees->applicant->referees as $referee)
                <tr>
                    <td>{{ $referee->name }}</td>
                    <td>{{ $referee->position }}</td>
                    <td>{{ $referee->company }}</td>
                    <td>{{ $referee->contact }}</td>
                </tr>
                @endforeach
            </table>

            <!-- Files Uploaded -->
            <h4 class="mt-4">Uploaded Documents</h4>
            <table class="table table-bordered">
                <tr>
                    <th>Document</th>
                    <th>Download</th>
                </tr>
                @foreach($document->applicant->documents as $document)
                <tr>
                    <td>{{ $document->name }}</td>
                    <td>
                        <a href="{{ asset($document->file_path) }}" class="btn btn-sm btn-primary" download>
                            Download
                        </a>
                    </td>
                </tr>
                @endforeach
            </table>